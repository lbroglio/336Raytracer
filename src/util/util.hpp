#ifndef R336_UTIL
#define R336_UTIL

#include "../world/worldObjects.hpp"

/**
 * @brief Calculate the distance in 3D space between two vertices
 * 
 * @param v1 Vertex to calculate the distance from v2
 * @param v2 Vertex to calculate the distance from v1
 * @return The distance between v1 and v2
 */
float dist3D(Vertex v1, Vertex v2);

#endif
